weaponIsLoaded = player.weaponIsLoaded();
if: weaponIsLoaded {
    # width, length, red, green, blue, alpha, useTexture
    runFunction(4, 325, 255, 255, 255, 255, true, "BUILTIN_fireLaser");
    #playSound("laser_pistol");
}